name: Test Reusable
on:
  workflow_call:
    inputs:
      skip_llm:
        required: false
        default: 'false'
        type: string
    secrets:
      ANTHROPIC_API_KEY:
        required: false
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo 'checkout works' && ls
